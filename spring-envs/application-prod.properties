# =============================================================================
# APPLICATION-PROD.PROPERTIES - PRODUCTION ENVIRONMENT ONLY
# =============================================================================
# File này CHỈ chứa NON-SENSITIVE config cho production
# SENSITIVE DATA (DB credentials, API keys) được inject từ Jenkins Credentials
# =============================================================================

# Kafka - Production Environment
# ⚠️ Có thể override bằng env var: SPRING_KAFKA_BOOTSTRAP_SERVERS
spring.kafka.bootstrap-servers=42.112.38.103:9092

# Flyway - Production Migration
spring.flyway.locations=classpath:db/migration/production

# External Services - Production URLs
# ⚠️ Có thể override bằng env vars nếu cần
convert-pdf.url=http://192.168.20.82:9013/esign/v1.0/convert-pdf
econtract-gateway.url=https://econtract.finy.vn

# Production-specific settings
# Giảm log level cho production
logging.level.root=warn
logging.level.com.technology.apigateway=info
spring.jpa.show-sql=false

# =============================================================================
# DATABASE CREDENTIALS: KHÔNG để ở đây!
# Sẽ được inject từ Jenkins Credentials qua environment variables:
# - SPRING_DATASOURCE_URL
# - SPRING_DATASOURCE_USERNAME  
# - SPRING_DATASOURCE_PASSWORD
# =============================================================================
